declare module "qml-fontawesome-raub" {
	/**
	 * Path to this module
	 *
	 * This is what you add to QML lib dirs, so that `import FontAwesome` works.
	 * The value is the same as `require.resolve('qml-fontawesome-raub')`.
	 */
	export const absPath: string;
}
